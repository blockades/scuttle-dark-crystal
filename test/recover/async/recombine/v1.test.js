const { describe } = require('tape-plus')

describe('recombine.async.recombine (v1)', context => {
  context('', assert => {
    assert.fail('WRITE TESTS - we still need to ensure we can recombine v1 shards')
  })
})
